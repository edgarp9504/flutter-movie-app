import 'package:flutter/material.dart';

class MovieDetailsPages extends StatelessWidget {
  const MovieDetailsPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
