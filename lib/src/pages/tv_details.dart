import 'package:flutter/material.dart';

class TvDetailsPages extends StatelessWidget {
  const TvDetailsPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
